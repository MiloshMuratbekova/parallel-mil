# Лабораторная работа: Гетерогенная параллелизация

Этот проект содержит решения четырех задач по гетерогенной параллелизации с использованием C++, OpenMP и CUDA.

## Структура проекта

```
parallel-mil/
├── task1/           # Задача 1: Теоретическое введение
│   └── answer.txt   # Ответ на теоретические вопросы
├── task2/           # Задача 2: Работа с массивами и OpenMP
│   ├── main.cpp     # Поиск min/max с параллелизацией
│   └── Makefile     # Файл для сборки
├── task3/           # Задача 3: Параллельная сортировка с OpenMP
│   ├── main.cpp     # Сортировка выбором с параллелизацией
│   └── Makefile     # Файл для сборки
└── task4/           # Задача 4: Сортировка на GPU с CUDA
    ├── main.cu      # Сортировка слиянием на GPU
    └── Makefile     # Файл для сборки
```

## Требования

### Для задач 2 и 3 (OpenMP)
- Компилятор g++ с поддержкой C++17
- Поддержка OpenMP (обычно идет с g++)

### Для задачи 4 (CUDA)
- NVIDIA CUDA Toolkit (версия 10.0 или выше)
- NVIDIA GPU с поддержкой CUDA
- Компилятор nvcc (входит в CUDA Toolkit)

## Установка зависимостей

### Ubuntu/Debian
```bash
# Для OpenMP
sudo apt-get update
sudo apt-get install g++ build-essential

# Для CUDA (если есть NVIDIA GPU)
# Скачайте CUDA Toolkit с сайта NVIDIA:
# https://developer.nvidia.com/cuda-downloads
```

### macOS
```bash
# Для OpenMP
brew install gcc libomp

# CUDA на macOS не поддерживается начиная с версии 10.14
# Используйте Linux или Windows для задачи 4
```

### Windows
- Установите Visual Studio с C++ компонентами
- Для OpenMP: уже включен в Visual Studio
- Для CUDA: скачайте CUDA Toolkit с сайта NVIDIA

## Как запустить

### Задача 1: Теоретическое введение
Просто откройте и прочитайте файл:
```bash
cd task1
cat answer.txt
```

### Задача 2: Работа с массивами и OpenMP
```bash
cd task2
make           # Компиляция
./task2        # Запуск
make clean     # Очистка
```

**Что делает программа:**
- Создает массив из 10,000 случайных чисел
- Находит минимум и максимум двумя способами:
  - Последовательно (один поток)
  - Параллельно (несколько потоков с OpenMP)
- Сравнивает время выполнения
- Показывает ускорение

### Задача 3: Параллельная сортировка с OpenMP
```bash
cd task3
make           # Компиляция
./task3        # Запуск
make clean     # Очистка
```

**Что делает программа:**
- Реализует сортировку выбором (selection sort)
- Тестирует на массивах размером 1,000 и 10,000 элементов
- Сравнивает последовательную и параллельную версии
- Объясняет проблемы параллелизации этого алгоритма

### Задача 4: Сортировка на GPU с CUDA
```bash
cd task4
make           # Компиляция
./task4        # Запуск
make clean     # Очистка
```

**Что делает программа:**
- Реализует параллельную сортировку слиянием на GPU
- Тестирует на массивах размером 10,000 и 100,000 элементов
- Сравнивает производительность GPU и CPU
- Показывает ускорение от использования GPU

**Примечание:** Для запуска задачи 4 необходима NVIDIA GPU!

## Понимание кода

### Задача 2: OpenMP директивы

```cpp
#pragma omp parallel for reduction(min:min_val) reduction(max:max_val)
```
- `parallel for` - распараллеливает цикл
- `reduction` - каждый поток находит свой min/max, затем они объединяются

### Задача 3: Критические секции

```cpp
#pragma omp critical
{
    // Только один поток может быть здесь одновременно
}
```

### Задача 4: CUDA концепции

```cpp
__global__ void kernel(...)  // Функция, выполняемая на GPU
kernel<<<blocks, threads>>>  // Запуск на GPU
cudaMemcpy(...)              // Копирование CPU ↔ GPU
```

## Ожидаемые результаты

### Задача 2
- Для 10,000 элементов: небольшое ускорение (1.2-2x) или даже замедление
- Накладные расходы на создание потоков могут превышать выгоду
- На больших массивах (миллионы элементов) эффект будет больше

### Задача 3
- Для 1,000 элементов: вероятно замедление
- Для 10,000 элементов: небольшое ускорение или примерно одинаково
- Сортировка выбором плохо параллелится из-за зависимостей

### Задача 4
- Для 10,000 элементов: GPU может быть медленнее из-за копирования данных
- Для 100,000 элементов: GPU должен показать преимущество
- Многое зависит от конкретной GPU и CPU

## Часто задаваемые вопросы

**Q: Программа с OpenMP падает или не компилируется**
A: Убедитесь, что используете флаг `-fopenmp` при компиляции

**Q: CUDA программа не компилируется**
A: Проверьте, что установлен CUDA Toolkit и nvcc доступен в PATH

**Q: "CUDA device not found"**
A: У вас нет NVIDIA GPU или не установлены драйверы CUDA

**Q: Параллельная версия медленнее последовательной**
A: Это нормально для малых задач - накладные расходы на параллелизацию
   превышают выгоду. Попробуйте увеличить размер массива.

**Q: Как изменить количество потоков OpenMP?**
A: Установите переменную окружения:
```bash
export OMP_NUM_THREADS=4
./task2
```

**Q: Как посмотреть, какая у меня GPU?**
A: Выполните команду:
```bash
nvidia-smi           # Linux/Windows
system_profiler SPDisplaysDataType  # macOS (но CUDA не поддерживается)
```

## Дополнительные эксперименты

1. **Измените размер массива** в исходном коде:
   - Попробуйте 100,000 или 1,000,000 элементов
   - Посмотрите, как меняется ускорение

2. **Измените количество потоков OpenMP**:
   - `export OMP_NUM_THREADS=2` (или 4, 8, 16)
   - Постройте график зависимости времени от числа потоков

3. **Попробуйте другие алгоритмы сортировки**:
   - Быстрая сортировка
   - Сортировка слиянием на CPU
   - Сравните их параллелизуемость

4. **Оптимизируйте CUDA код**:
   - Используйте shared memory
   - Экспериментируйте с размерами блоков
   - Минимизируйте копирование данных

## Ресурсы для изучения

- [OpenMP Documentation](https://www.openmp.org/)
- [CUDA Programming Guide](https://docs.nvidia.com/cuda/cuda-c-programming-guide/)
- [Introduction to Parallel Programming (Udacity)](https://www.udacity.com/course/intro-to-parallel-programming--cs344)

## Форматирование кода

Для форматирования кода используется стандартный стиль C++:
- Отступы: 4 пробела
- Фигурные скобки: на той же строке
- Понятные имена переменных
- Комментарии на русском языке для понимания

Можно использовать clang-format:
```bash
clang-format -i main.cpp  # Автоформатирование
```

## Авторство

Этот проект создан в учебных целях для изучения гетерогенной параллелизации.
Код написан простым и понятным языком для студентов, впервые знакомящихся с C++ и параллельным программированием.

---
**Удачи в изучении параллельного программирования! 🚀**
